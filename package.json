{
  "name": "ascent",
  "main": "src/index.ts",
  "dependencies": {
    "@sapphire/framework": "latest",
    "discord.js": "14.x"
  },
  "scripts": {
    "start": "node src/index.ts",
    "dev": "ts-node-dev --respawn --clear --debug --poll -r tsconfig-paths/register src/index.ts"
  },
  "devDependencies": {
    "ts-node-dev": "^2.0.0",
    "tsconfig-paths": "^4.2.0"
  }
}
